<UserControl
    x:Class="ZoDream.Shop.Controls.BarItem"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ZoDream.Shop.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    x:Name="root"
    d:DesignHeight="48"
    d:DesignWidth="48">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <FontIcon Glyph="{Binding Icon,ElementName=root}" FontFamily="/Resources/iconfont.ttf#iconfont" FontSize="{Binding FontSize,ElementName=root}"/>
        <TextBlock Text="{Binding Label,ElementName=root}" VerticalAlignment="Center" Grid.Row="1" FontSize="12" TextAlignment="Center"/>
    </Grid>
</UserControl>
